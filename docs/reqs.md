# Требования к "Информационному радару"

## 1. Цель

Создать интерактивное консольное приложение для агрегации, фильтрации, постобработки контента из различных настраиваемых источников (например, Habr, TJ, Telegram, Medium, Reddit и т.д.) для ускорения обработки актуальной информации по интересующим направлениям.

## 2. Ключевые концепции

### 2.1. Источник (Source)
**Источник** - это внешний ресурс, поставляющий контент (статьи, посты). Каждый источник имеет уникальное имя (например, `habr`) и свой тип, определяющий логику сбора и парсинга данных. Источники настраиваются в `config.yml`.

### 2.2. Раздел (Topic)
**Раздел** - это тематическая категория внутри источника. Для Habr это "хаб" (e.g., `artificial_intelligence`), для Reddit - "сабреддит". Название "Раздел" используется как универсальный термин в интерфейсе.

### 2.3. Статья и ее метаданные
**Статья** - TBD

## 3. Функциональные требования

### 3.1. Конфигурация и Хранение

#### 3.1.1. Файл конфигурации (`config.yml`)
Структура должна быть обновлена для поддержки нескольких источников.

**Пример:**
```yaml
sources:
  habr:
    type: habr # Тип провайдера для обработки
    hubs:
      - artificial_intelligence
      - python
    rss_template: "https://habr.com/ru/rss/hubs/{hub_slug}/articles/?limit=100"
    initial_fetch_date: "2025-11-16 00:00:00"
    auto_update_within_days: 3
    display:
      article_meta:
        - { field: "extra_data", key: "rating", icon: "★" }
        - { field: "extra_data", key: "views_count", icon: "👁" }
        - { field: "comments_data", key: "count", icon: "💬" }
  # В будущем можно будет добавить:
  # reddit_python:
  #   type: reddit
  #   subreddit: Python
  #   ...
```

#### 3.1.2. База данных (`inforadar.db`)
*   В таблицу `Article` необходимо добавить новое поле:
    *   `source: str` - имя источника, из которого была получена статья (e.g., 'habr').

### 3.2. Интерактивный интерфейс (UI/UX)

#### Страница 1: Список источников (Sources)
*   **Содержимое:**
    *   Представляет собой таблицу с колонками: `Источник`, `Кол-во статей`, `Дата последней статьи`.
    *   Реализована постраничная навигация (pagination), если список не помещается на один экран.
    *   В футере постоянно отображается строка состояния с подсказками по командам и номером текущей страницы (`Стр. X/Y`).
*   **Действия (шорткаты):**
    *   `↑/↓`: Перемещение курсора выбора по строкам.
    *   `←/→` (или `PgUp/PgDown`): Переключение между страницами списка.
    *   `Enter`: Перейти на страницу разделов для выбранного источника.
    *   `f`: Запустить операцию сбора новых статей **по всем источникам**.
    *   `q`: Выход из приложения.

#### Страница 2: Список разделов источника (Topics)
*   **Содержимое:**
    *   В заголовке отображаются "хлебные крошки" для навигации (например, `Главная > habr`).
    *   Представляет собой таблицу с колонками: `Раздел`, `Кол-во статей`, `Дата последнего обновления`.
    *   Реализована постраничная навигация и имеется футер с подсказками и счетчиком страниц.
*   **Действия (шорткаты):**
    *   `↑/↓`: Перемещение курсора выбора по строкам.
    *   `←/→` (или `PgUp/PgDown`): Переключение между страницами.
    *   `Enter`: Перейти на страницу статей для выбранного раздела.
    *   `f`: Запустить операцию сбора статей **только для текущего источника**.
    *   `Esc`: Вернуться на предыдущую страницу (список источников).

#### Страница 3: Список статей раздела (Articles)
*   **Содержимое:**
    *   В заголовке отображаются "хлебные крошки" (например, `Главная > habr > artificial_intelligence`).
    *   Представляет собой таблицу с колонками, например: `ID`, `Заголовок`, `Дата`, `Мета`.
    *   Колонка `Мета` динамически формируется на основе правил из секции `display.article_meta` в `config.yml`, отображая иконки и значения (например, `★+12 👁2.1k 💬42`).
    *   Реализована постраничная навигация и имеется футер с подсказками.
*   **Действия (шорткаты):**
    *   `↑/↓`: Перемещение курсора выбора по строкам.
    *   `←/→` (или `PgUp/PgDown`): Переключение между страницами.
    *   `Enter`: Открыть страницу просмотра для выбранной статьи.
    *   `o`: Открыть оригинальную статью в браузере.
    *   `i`: Отметить статью как "интересную".
    *   `d`: Отметить статью как "прочитанную" (скрыть из основного списка).
    *   `Esc`: Вернуться на предыдущую страницу (список разделов).

#### Страница 4: Просмотр статьи (Article Preview)
*   **Содержимое:** Развернутая информация о статье: заголовок, метаданные, отформатированный текст (Markdown).
*   **Действия:**
    *   `↑/↓` или `j/k`: Прокрутка текста статьи.
    *   `→/←` (стрелки вправо/влево): Переключение на следующую/предыдущую статью в списке, не выходя со страницы просмотра.
    *   `Esc`: Вернуться на предыдущую страницу (список статей).

### 3.3. Сбор и обработка данных

#### Операция `fetch` (Сбор/Обновление)
*   Больше не является отдельной CLI-командой. Запускается по шорткату `f`.
*   **Контекст вызова:**
    *   На странице источников: запускает `fetch` для **всех** источников.
    *   На странице разделов: запускает `fetch` для **выбранного** источника.
*   **Логика:**
    *   Сохраняет и использует `initial_fetch_date` для первого сбора.
    *   Автоматически обновляет метаданные для свежих статей (`auto_update_within_days`).
    *   Отображает наглядный прогресс выполнения операции.

---

## 4. Нефункциональные требования
*   **Адаптивность интерфейса:** Приложение должно использовать специализированную TUI-библиотеку (например, `rich` для Python) для корректной отрисовки и автоматического изменения размеров таблиц и элементов интерфейса при ресайзе окна консоли.
*   **Устойчивость:** Корректная обработка сетевых ошибок и изменений верстки без падения приложения.
*   **Производительность:** Интерфейс должен оставаться отзывчивым во время операций сбора данных (использование асинхронного подхода).
*   **User-Agent:** Использование корректного User-Agent при запросах к внешним ресурсам.